# 进行中

## 项目知识结构

- 正在建立 `项目地图.md`、领域功能页、任务进展和规则页。
- 完成标准：入口可定位、文件规模小、内容与当前源码一致。

## 未归属工作区状态

- `app/.../ui/components/message/ChatMessageActions.kt` 当前显示为未提交修改，但普通 diff 未显示有效内容，可能是行尾或工作区元数据变化。
- `tmp/` 为未跟踪目录。

处理原则：在来源和目的确认前，不提交、不恢复、不删除上述内容。

## 主动语音通话

- 当前工作区已加入助手级主动通话工具、专用提示词、来电震动、滑动接听、拒接和 30 秒未接反馈。
- 接听后复用现有 `VoiceCallOverlay` 与输入法语音链路；拒接和未接结果通过工具审批状态返回 AI。
- 已通过 `:app:compileDebugKotlin` 和 `:app:testDebugUnitTest`，尚未提交或打包。
- 助手设置已将“朋友圈”“匿名提问箱”开关迁入 Local Tools，并将“朋友圈聊天模式”显示名缩短为“聊天模式”。
