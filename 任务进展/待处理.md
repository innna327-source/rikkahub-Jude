# 待处理

## 发布链路

- 将 `UpdateChecker.kt` 的 GitHub API 地址从旧仓库 `rikkahub-auto-compress` 更新为 `rikkahub-Jude`；旧 APK 当前依赖 GitHub 重定向。
- README 中仍有旧仓库链接，发布新版本前应统一更新。
- 下次面向旧用户发布时提升到高于 `v1.0.8` 的版本，并在 Latest Release 上传名为 `app-universal-debug.apk` 的资产。

## 朋友圈可靠性

- 到期处理依赖打开或刷新朋友圈；若需要后台准时回复，应另行设计 WorkManager/前台任务，不能直接复用 UI 生命周期。

## 文档维护

- 功能或模块边界变化后，同步更新对应领域页，不在 `项目地图.md` 堆叠实现细节。
- 每次版本发布后更新 `已完成.md` 的版本和提交事实。
